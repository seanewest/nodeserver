var assert = require('assert');
var test = require('./tester');
var createServer = require('./server');

test('false is false', function() {
  assert(false===false, 'false should be false');
});

test('false is false', function() {
  assert(false===false, 'false should be false');
});

test('false is false', function() {
  assert(false===false, 'false should be false');
});

test('2 + 2 = 5', function is5() {
  assert(2+2===5, 'it should be 5');
});

test('3 + 3 = 6', function is6() {
  assert(3+3===6, 'it should be 6');
});

test('false is false', function() {
  assert(false===false, 'false should be false');
});

test('false is false', function() {
  assert(false===false, 'false should be false');
});

test('division by 0', function div0() {
  sdlkfjl
});

test('3 + 4 = 7', function is7() {
  assert(3+4===7, 'it should be 7');
});

test('false is false', function() {
  assert(false===false, 'false should be false');
});

test('false is false', function() {
  assert(false===false, 'false should be false');
});
